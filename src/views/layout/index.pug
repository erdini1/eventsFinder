doctype html
html(lang="en")
	head
		meta(charset="UTF-8")
		meta(name="viewport", content="width=device-width, initial-scale=1.0")
		title Eventos | #{titlePage}
		link(rel="stylesheet", href="/css/app.css")
		block styles
	body(class="min-h-screen bg-gray-50")

		if navbar
			include header

		main(class="mx-auto container mt-2 px-2")
			block content

		block scripts